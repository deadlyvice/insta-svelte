import presetIcons from '@unocss/preset-icons'
import { defineConfig } from 'unocss'

export default defineConfig({
	presets: [
		presetIcons({
			// Optional: Add options here if needed, e.g., for customizing icons
		})
		// ...other presets
	]
})
